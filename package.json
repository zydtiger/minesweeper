{
  "name": "minesweeper",
  "version": "1.0.0",
  "main": "app.js",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.1",
    "serve-handler": "^6.1.3"
  },
  "scripts": {
    "start": "node .",
    "compilets": "tsc -p . && browserify src/build/index.js -o src/build/bundle.js && minify src/build/bundle.js > bundle.min.js",
    "less": "lessc src/less/style.less src/build/style.css & minify src/build/style.css > style.min.css",
    "build": "yarn compilets && yarn less"
  }
}
